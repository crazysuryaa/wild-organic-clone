const AnnouncementBar = () => {
  return (
    <div className="bg-announcement text-announcement-foreground text-center py-2 text-sm font-medium">
      Free shipping for all orders of $49+ within the contiguous U.S.
    </div>
  );
};

export default AnnouncementBar;
